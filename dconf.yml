name: tensorenv
channels:
 - defaults
 - conda-forge
dependencies:
 - python=3.8.5
 - anaconda 
 - pip==20.1.1
 - pip:
    - azureml-sdk
    - azureml-defaults
    - tensorflow-cpu==2.5.0
    - numpy==1.19.3
    - pillow 